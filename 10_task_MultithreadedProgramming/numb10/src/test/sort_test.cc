#include <gtest/gtest.h>

using namespace std;
TEST(merge, sort) {
        vector<float> x;
        merge_sort_multithread(arr2, buffer, threads_count);
        EXPECT_EQ(0u, x.size());
        x.push_back(1);
        EXPECT_EQ(1u, x.size());
}
